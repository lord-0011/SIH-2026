# notebooks/ — EDA only

Exploration lives here (esp. STEP_05 feasibility gate). But every number that lands in a
doc must be reproducible by a named cell/script (docs/09 Rule 2). Anything load-bearing
graduates into src/ with a test — notebooks are not the source of truth.
