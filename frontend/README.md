# frontend/ — React dashboard (STEP_14, Phase 15)

Vite + React + Tailwind + Recharts. Consumes the FastAPI backend (src/api).
Three levels: National → Sector/Ministry → Project. No mock data in the demo build.
Scaffold created at STEP_14 (not before — nothing to serve until the API exists).
